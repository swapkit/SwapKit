pre-commit:
  commands:
    lint:
      glob: "*.{jsx,tsx,ts,js,json}"
      run: bun biome check --write {staged_files} && git add {staged_files}
    type-check:
      glob: "*.{ts,tsx}"
      run: bun type-check
